meta {
  name: Log in
  type: http
  seq: 1
}

post {
  url: {{host}}/authentication
  body: json
  auth: none
}

body:json {
  {
    "strategy": "local",
    "username": "brunomars",
    "password": "24kmagic"
  }
}

vars:post-response {
  jwtToken: res.body.accessToken
}

docs {
  ## POST /authentication
  
  - Create a test user before logging in.
  - Once you're logged in, the access token will be saved in a variable `jwtToken` which can be used in other requests.
}
