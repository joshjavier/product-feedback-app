meta {
  name: Find feedback requests by status
  type: http
  seq: 2
}

get {
  url: {{host}}/requests?status=suggestion
  body: none
  auth: none
}

params:query {
  status: suggestion
}

vars:post-response {
  requestId: res.body.data[0]._id
}
